# Frontend

1、npm install

2、web端调试 / 打包：`npm run dev` / `npm run build`

3、桌面端调试 / 打包 `npm run electron:dev` / `npm run electron:build`

# Backend

1、环境安装：`pip install -r requirments.txt`

2、`.env.example`填写api-key, 改名为 `.env`

3、运行后端服务：`python main.py`
